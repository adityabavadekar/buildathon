# Copy to .env.local for local overrides. Never put a secret here: anything
# NEXT_PUBLIC_-prefixed ships in the browser bundle.

# Where next.config.ts proxies /api/* to. Read at BUILD time, not runtime -
# Next bakes the rewrite destination into routes-manifest.json.
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
