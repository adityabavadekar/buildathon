# Port to listen on
PORT=3099

# Optional: protect management routes with a shared secret.
# Pass as x-relay-secret header or ?secret=... query param.
# Leave empty to disable auth.
RELAY_SECRET=change_me_to_something_random
